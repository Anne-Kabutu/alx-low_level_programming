#include "main.h"

/**
 * mul - multiplies two ints
 * @a: int to multiply
 * @b: int to multiply by
 *
 * Return: result of a*b
 */

int mul(int a, int b)
{
	int mul;

	mul = a * b;

	return (mul);
}
