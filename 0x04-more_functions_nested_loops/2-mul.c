#include "main.h"

/**
 * mul - muliplies two integers
 * @a: first integer to be multiplied
 * @b: iteger to be multiplied
 *
 * Return: mul
 */
int mul(int a, int b)
{
	int mul = a * b;
		return (mul);
}
