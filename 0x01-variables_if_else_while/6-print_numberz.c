#include <stdio.h>

/**
 * main - prints nums to base 10
 *
 * Return: always 0
 */

int main(void)
{
	int num = 48;

	while (num <= 57)
	{
		putchar(num);
		num++;
	}

	putchar('\n');

	return (0);
}
